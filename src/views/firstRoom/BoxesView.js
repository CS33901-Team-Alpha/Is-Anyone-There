class BoxesView extends SlidingDoorView{
  constructor(slidingDoors = []) {
    super(slidingDoors, SM.get("EastWall"));
  }

  update(dt) {
    super.update(dt)
  }

  draw() {
    super.draw()
  }

  onEnter() {
    super.onEnter();
  }

  onExit() {
    super.onExit();
  }
}
